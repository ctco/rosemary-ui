import React from 'react';
import {TextArea} from '../../../src';
import OptionsTable from '../../helper/OptionsTable';


export default () => {
    let propDescription = {
    };

    return (
        <OptionsTable component={TextArea} propDescription={propDescription} />
    );
};
