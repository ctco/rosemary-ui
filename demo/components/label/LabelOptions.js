import React from 'react';
import {Label} from '../../../src';
import OptionsTable from '../../helper/OptionsTable';

export default () => {
    let propDescription = {
    };

    return (
        <OptionsTable component={Label} propDescription={propDescription} />
    );
};
